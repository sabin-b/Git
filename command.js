/**
 * commands
 *
 * 1) git rm --cached index.js - it will bring staged files (from staging area) to unstaging area ( not tracking files)
 *
 * 2) git restore --staged filename or period (.) - for example: already file.js is in local repositary,now we have made some changes right now, now if we thought discard the changes which you made right now, that's time we can use this
 */
